# 🏆 منصة مسابقات تكنوفيست

## نظرة عامة
منصة لإدارة مسابقات تكنوفيست باللغة العربية، تتضمن تسجيل الطلاب، إدارة المشاريع، وتقييمها.

---

## 📁 هيكل المشروع

```
technofest-platform/
├── backend/                 # Python FastAPI
│   ├── main.py             # نقطة البداية
│   ├── database.py         # إعدادات قاعدة البيانات
│   ├── models.py           # نماذج البيانات
│   ├── schemas.py          # مخططات Pydantic
│   ├── routers/            # مسارات API
│   │   ├── students.py     # مسارات الطلاب
│   │   ├── projects.py     # مسارات المشاريع
│   │   ├── admin.py        # مسارات الإداريين
│   │   └── evaluation.py   # مسارات التقييم
│   ├── services/           # الخدمات
│   │   ├── email_service.py
│   │   ├── ai_evaluation.py
│   │   └── pdf_generator.py
│   └── requirements.txt
│
├── frontend/               # React
│   ├── src/
│   │   ├── components/     # المكونات
│   │   ├── pages/          # الصفحات
│   │   └── styles/         # التنسيقات
│   ├── package.json
│   └── index.html
│
└── docs/                   # التوثيق
```

---

## 🚀 خطوات التشغيل

### الخطوة 1: إعداد قاعدة البيانات PostgreSQL

```bash

createdb technofest_db
```

### الخطوة 2: تشغيل الباك إند

```bash
cd backend
pip install -r requirements.txt
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

### الخطوة 3: تشغيل الفرونت إند

```bash
cd frontend
npm install
npm run dev
```

---

## 📋 المتطلبات

### سيناريوهات التسجيل:
1. ✅ فريق لديه فكرة مشروع
2. ✅ فرد لديه فكرة مشروع  
3. ✅ فرد بدون فكرة مشروع
4. ✅ فريق بدون فكرة مشروع

### استبيان المشروع:
- عنوان المشروع
- المشكلة التي يحلها
- الوصف التقني (1000+ حرف)
- المرجع العلمي
- رفع صور/مخططات

### لوحة الإداريين:
- تسجيل دخول آمن
- عرض وتصدير الاستبيانات PDF
- فرز الأفراد إلى فرق
- إرسال روابط تلغرام بالبريد
- تقييم المشاريع (50% إداريين + 50% AI)
- عرض أفضل 5 فرق

---

## 🔧 المتغيرات البيئية

```env
DATABASE_URL=postgresql://user:password@host:5432/technofest_db
SECRET_KEY=your-secret-key-here
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
OPENAI_API_KEY=your-openai-key  # للتقييم بالذكاء الاصطناعي
```